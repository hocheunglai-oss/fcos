<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Sell_At__c</fullName>
    <formula>IF(
    Fixed__c,

    BLANKVALUE(Lumpsum_Sell_At__c, 0),

        IF(
            ISBLANK(Minimum_Sell_At__c) ||
            (Quantity * UnitPrice &gt; Minimum_Sell_At__c),
            Quantity * UnitPrice,
            Minimum_Sell_At__c
        )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Total Sell At</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
